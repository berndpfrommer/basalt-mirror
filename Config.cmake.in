@PACKAGE_INIT@

include ( "${CMAKE_CURRENT_LIST_DIR}/BasaltTargets.cmake" )

set_and_check(BASALT_INCLUDE_DIRS "@PACKAGE_INCLUDE_INSTALL_DIR@")
set_and_check(BASALT_SYSCONFIG_DIR "@PACKAGE_SYSCONFIG_INSTALL_DIR@")

# set(BASALT_LIBRARIES basalt)
set(BASALT_LIBRARIES ${EXPORTED_LIBRARIES})

check_required_components(Basalt)